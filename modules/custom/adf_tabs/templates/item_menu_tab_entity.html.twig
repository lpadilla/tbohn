{#
/**
 * @file item_menu_tab_entity.html.twig
 * Default theme implementation to present Item menu tab entity data.
 *
 * This template is used when viewing Item menu tab entity pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_item_menu_tab_entity()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('item_menu_tab_entity') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
