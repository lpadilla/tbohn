{#
/**
 * @file group_account_relations.html.twig
 * Default theme implementation to present Company user relations data.
 *
 * This template is used when viewing Company user relations pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_group_account_relations()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('group_account_relations') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
